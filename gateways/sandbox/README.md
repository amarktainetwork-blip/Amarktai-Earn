# Sandbox Broker

Planned controller-only broker for launching disposable non-root job containers with CPU/RAM/PID limits, isolated volumes, no Docker socket in the job container, no master credentials, and restricted networking.
